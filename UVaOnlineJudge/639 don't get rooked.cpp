#include<bits/stdc++.h>
#define in '#';
#define max=5;
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int i, n;
    while(cin >> n && n){
        cin >> ws;
        char mt[]
        for(i=0; i<n; i++) scanf(" %s ", &mat[i]);
        memset(mark, false, sizeof(mark));
        res=0;
        bt(0, 0, 0);
        cout << res << endl;
    }
    return 0;
}